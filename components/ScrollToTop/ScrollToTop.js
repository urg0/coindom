/* import { useEffect } from "react";
import { useRouteNode } from "react-router-dom";

function ScrollToTop() {
  const { route } = useRouteNode("");

  useEffect(() => {
    window.scrollTo(0, 0);
  }, [route]);

  return null;
}

export default ScrollToTop;
 */
